Place local generated assets here when working with the public repo.

Expected examples include the top-100k JSON files, MixDNA lite export files, and any intermediate artifacts required by the build scripts.

This directory is ignored by git on purpose.
